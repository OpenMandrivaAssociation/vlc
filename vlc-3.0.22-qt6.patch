diff -up vlc-3.0.22-rc1/configure.ac.omv~ vlc-3.0.22-rc1/configure.ac
--- vlc-3.0.22-rc1/configure.ac.omv~	2025-10-04 19:30:27.884270213 +0200
+++ vlc-3.0.22-rc1/configure.ac	2025-10-04 19:31:02.983999105 +0200
@@ -3852,18 +3852,18 @@ AC_ARG_ENABLE(qt, [
 ])
 have_qt5_x11="no"
 AS_IF([test "${enable_qt}" != "no"], [
-  PKG_CHECK_MODULES([QT], [Qt5Core >= 5.5.0 Qt5Widgets Qt5Gui Qt5Svg], [
-      PKG_CHECK_MODULES([QT5_X11], [Qt5X11Extras], [
+  PKG_CHECK_MODULES([QT], [Qt6Core >= 5.5.0 Qt6Widgets Qt6Gui Qt6Svg], [
+      PKG_CHECK_MODULES([QT5_X11], [Qt6X11Extras], [
           have_qt5_x11="yes"
       ],[
           AC_MSG_WARN([Not building Qt Interface with X11 helpers.])
       ])
-      QT_PATH="$(eval $PKG_CONFIG --variable=exec_prefix Qt5Core)"
-      QT_HOST_PATH="$(eval $PKG_CONFIG --variable=host_bins Qt5Core)"
-      QT_VERSION="$(eval $PKG_CONFIG --modversion Qt5Gui)"
-      AC_PATH_PROGS(MOC, [moc-qt5 moc], moc, ["${QT_HOST_PATH}" "${QT_PATH}/bin"])
-      AC_PATH_PROGS(RCC, [rcc-qt5 rcc], rcc, ["${QT_HOST_PATH}" "${QT_PATH}/bin"])
-      AC_PATH_PROGS(UIC, [uic-qt5 uic], uic, ["${QT_HOST_PATH}" "${QT_PATH}/bin"])
+      QT_PATH="$(eval $PKG_CONFIG --variable=exec_prefix Qt6Core)"
+      QT_HOST_PATH="$(eval $PKG_CONFIG --variable=host_bins Qt6Core)"
+      QT_VERSION="$(eval $PKG_CONFIG --modversion Qt6Gui)"
+      AC_PATH_PROGS(MOC, [moc-qt6 moc], moc, ["${QT_HOST_PATH}" "${QT_PATH}/bin"])
+      AC_PATH_PROGS(RCC, [rcc-qt6 rcc], rcc, ["${QT_HOST_PATH}" "${QT_PATH}/bin"])
+      AC_PATH_PROGS(UIC, [uic-qt6 uic], uic, ["${QT_HOST_PATH}" "${QT_PATH}/bin"])
     ], [
       AS_IF([test -n "${enable_qt}"],[
         AC_MSG_ERROR([${QT_PKG_ERRORS}.])
